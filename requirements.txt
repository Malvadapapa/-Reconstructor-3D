# ==============================================================================
# Reconstructor Métrico 3D (RGB a STL) - Dependencias del Proyecto
# Autor: Cristian Vargas
# ==============================================================================

# Core Numeric, Matrix & Computer Vision
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
opencv-python>=4.8.0
pupil-apriltags>=1.0.4

# 3D Mesh, Spatial Indexing & Geometry Processing
open3d>=0.18.0
trimesh>=4.0.0
shapely>=2.0.0
rtree>=1.0.0
networkx>=3.0.0

# Web Application, Asynchronous API & Real-time Streaming
fastapi>=0.110.0
uvicorn>=0.28.0
python-multipart>=0.0.9
pydantic>=2.6.0
jinja2>=3.1.3
httpx>=0.27.0
sse-starlette>=2.0.0

# Utilities & Milimetric PDF Report / Marker Generation
matplotlib>=3.8.0
reportlab>=4.1.0
pyyaml>=6.0.1
tqdm>=4.66.0
requests>=2.31.0

# Neural Matching & Dense Reconstruction (develop branch)
torch>=2.0.0                    # BSD-3 — backend para LightGlue/DISK
torchvision>=0.15.0             # BSD-3 — image transforms
lightglue @ git+https://github.com/cvg/LightGlue.git  # Apache-2.0 — matcher neuronal + extractor DISK
pycolmap>=0.6.0                 # BSD-3 — API Python de COLMAP (DB, mapper, MVS)

# UV Texture Mapping
xatlas>=0.0.9                   # MIT — UV unwrap paramétrico
pyembree>=0.1.6                 # Apache-2.0 — raycasting acelerado (opcional, mejora trimesh)
